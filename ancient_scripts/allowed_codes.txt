{
    /* WHERE RHS IS ANY PRIMARY EXPRESSION: (number, string, identifier) */

    let var_name = 0; // number 
    let variable_name = var_name; // identifier
    const variable_name_string = "hello"; // string
    const variable_name_again = (60 + 3 + foo);

    // The RHS is an EXPRESSION
    // In the analysis, we'll have it check for the types
}

{
    /* WHERE RHS IS EITHER A NUMBER OR AN IDENTIFIER */

    var_name += 0; // number
    variable_name -= var_name; // identifier
}

let var_name; // NOT ALLOWED! MUST HAVE A TYPE ATTACHED TO IT



// CONCERNING TYPES
// WHERE TYPE IS ANY TYPE EXPRESSION: (string, array)
// STRING BECAUSE USER CAN DEFINE THEIR OWN TYPES

let type_name: TYPE = ;
let 