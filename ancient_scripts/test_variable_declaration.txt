let a: Number = 90;

let b: String = "Hallo";

const c: []UserDefined = [XYZ, OOP];

let (d: Number, e: String, f:UserDefined) = (90, "Welcome", POP);







// pub fn start(src_code: String) {
//     let lexer = Lexer::new(src_code);

//     if !lexer.completed_without_errors() {
//         return; // if there are errors, go no further.
//     }

//     let parser = Parser::new(lexer);
//     let program = Program::new();

//     parse(parser, program, true);
// }